# Ask Documents — source-cited, on-device questions

## How to use

1. Select documents in an Explorer pane, then choose **File Tools → Ask Documents…**, or open **Ask AI → Ask Documents**. **Choose Documents…** also opens a native file picker.
2. Review the filenames and select **Read Documents**. Selecting files or opening the tool does not read their contents. PDF pages without embedded text are read with on-device English OCR; the active file and page are shown during recognition.
3. Ask a specific English question, such as `What is the payment deadline?` or `What is the invoice total?`.
4. Each answer statement includes a supporting quote and a source button. Click it to inspect the retrieved source text and its filename/PDF page, or reveal the file in Explorer.
5. Ask a follow-up such as `When was it issued?` or `What about Cedar Studio?`. **Interpreted as** shows the self-contained question used to retrieve fresh evidence. **New Conversation** forgets the conversation but keeps the already-read documents ready.

Only the explicit selection is used. A different nonempty Explorer selection replaces the previous document context when opening the tool and requires reading again. An empty selection allows the previous in-memory context to be reopened. Follow-ups use at most the last two successful resolved questions and their verified quotes. The last three answers are kept for inspection; older answers fall out of window memory. When ambiguity is detected, the tool asks the user to name the subject. This is not general chat or a whole-document summarizer.

## Supported inputs and retrieval limits

- **1–5** local PDF, TXT, MD, JSON or CSV files. Text formats must be valid UTF-8 without NUL bytes. No recursive folder reads, network volumes, file symlinks or cloud placeholders.
- At most **20 MB**, **100 PDF pages**, and **200,000 extracted text characters per file**, with **300,000 characters total**. Limit violations fail explicitly; the tool does not silently truncate a selected document or omit a failed selection.
- PDFKit preserves existing text. Only pages whose embedded text is empty or whitespace use OCR, including image-only pages in mixed PDFs. A page with some embedded text is not OCRed again, so additional text inside its images may be absent. Locked/malformed PDFs fail with an explanation.
- At most **20 pages needing OCR per selection**, including blank pages, and **20,000 recognized characters per page**. OCR shares the per-file and total-text limits above. A failed page or limit violation rejects that read rather than publishing partial context. Empty pages are counted and disclosed; a completely unreadable/blank document fails explicitly.
- Page-preserving chunks are up to 900 characters with 100-character overlap. They are divided into overlapping, contiguous **600 UTF-8 byte** excerpt windows before indexing, so evidence near the end of a chunk remains searchable. At most four distinct passage IDs enter the answer model. Filenames are not supporting evidence.
- After **Read Documents**, Apple's local English sentence embedding prepares vectors for those excerpts in memory. Each question embeds only its query and reuses the prepared source vectors. Ranking combines rare-word-weighted keyword matches with cosine similarity; semantic-only candidates currently require similarity of at least 0.22. This is a retrieval heuristic, not a probability, confidence score or evidence of truth. This threshold is exercised on a small synthetic English corpus, not established as a universal cutoff.
- If a local English embedding or query vector is unavailable, retrieval falls back to keywords and discloses the mode. The app never requests embedding asset downloads or uploads text. Each encoding batch owns one `NLEmbedding` instance, used serially off the main actor; instances are not shared concurrently.
- Questions and resolved questions are at most 500 characters / 1,000 UTF-8 bytes. Model output is constrained to at most three source-cited claims. Each generation has a 30-second cooperative cancellation deadline; a follow-up can require a resolution generation and an answer generation. No overlapping request starts while cancellation unwinds.

This is **hybrid keyword/semantic retrieval**, using the sentence-similarity approach described in Apple's [Finding similarities between pieces of text](https://developer.apple.com/documentation/naturallanguage/finding-similarities-between-pieces-of-text). The [sentence embedding API](https://developer.apple.com/documentation/naturallanguage/nlembedding/sentenceembedding(for:)) returns nil when unavailable; the tool then uses keywords. Synonyms/paraphrases can still miss relevant material, and the selected excerpts need not represent every part of every document. Absence of a retrieved answer is not proof the complete documents lack the information. No cross-file totals, comparisons or conclusions are intentionally inferred beyond the excerpts.

## Scanned PDFs

Recognition uses Apple's on-device [Vision text request](https://developer.apple.com/documentation/vision/recognizetextrequest), configured for accurate English recognition without automatic language detection or language correction. It does not use the answer model to guess missing text. This first version targets clear, printed English scans; handwriting, skewed/sideways scans, complex tables and low-quality images can be missed or misread. Reading order is Vision's recognized observation order, not a reconstructed table or page layout.

Each PDF snapshot is processed serially off the main actor. Pages are rendered at up to 3 pixels per PDF point and at most **2,400 pixels on either edge** (5.76 million pixels). Crop bounds and PDF rotation metadata are respected, using the [PDF drawing transform](https://developer.apple.com/documentation/coregraphics/cgpdfpage/getdrawingtransform(_:rect:rotate:preserveaspectratio:)) after explicit point-to-pixel scaling. This bounds the output bitmap, not every internal allocation PDFKit may make while decoding an embedded image. Every recognition request has a **15-second cooperative deadline**. Cancellation discards late text and progress; the tool stays busy until the underlying operation unwinds, so this is not a hard wall-clock kill of PDFKit/Vision.

Recognized passages retain their original PDF page and an **OCR** marker throughout retrieval and citation display. The ready summary reports how many pages yielded OCR text. OCR-backed answers and their source sheets warn users to check the original, especially names, dates and amounts. Quote validation checks the recognized transcript, **not its accuracy against the scanned pixels**. The source sheet displays the transcript; **Show in Explorer** reveals the original file, not a cropped image or automatic page jump.

PDF bytes are never rewritten, OCR layers are never inserted, and neither page rasters nor transcripts are saved in an app-created disk cache. Clear, selection changes and opt-out use the same memory-only lifecycle as native document text. This does not add background indexing, startup OCR, an external model download, or a cloud request.

## Source validation and safety

The model receives the question, numbered source excerpts and their filenames, but no filesystem paths. Filenames help distinguish subjects across selected documents; they are untrusted context, not quotable evidence. The prompt requests only the fact asked about the named subject, not a summary of unrelated excerpts. The app maps IDs back to the selected documents. Every cited source ID must exist, and every normalized quote must be a contiguous substring of its retrieved source. Unknown IDs, fabricated/paraphrased quotes, empty claims or insufficient evidence reject the answer. No unverified new answer is shown; a previous verified answer can remain under its original question. Retrieved passages identify the question they belong to.

**An existing quote does not prove the model's statement logically follows from it.** The UI explicitly asks users to check the supporting quotes. This is not a guarantee of factual correctness or a substitute for reviewing important documents.

The follow-up resolver receives at most two resolved questions and two verified quotes per turn (300 UTF-8 bytes per quote). Generated answer statements, filesystem paths and arbitrary session transcripts are not supplied as evidence. It uses a fresh, tool-free on-device session (240 output tokens) to reconstruct the question, not answer it. The answerer then uses another fresh session (650 output tokens) with newly retrieved excerpts. Previous answers are never simply repeated as proof. A failed or cancelled request does not advance conversation history. Explicit new subjects should replace old ones; the visible interpretation allows the user to check the model's choice. Ambiguity detection and subject resolution are still model judgments, not guarantees.

A narrow deterministic English guard also rejects singular `it`/`its`/`this`/`that` references after explicit list, comparison or recognized plural-subject questions. This prevents selecting the first invoice after a question about multiple studios, but is not a general-purpose coreference validator.

Question/document strings are untrusted data in a bounded prompt, never trusted instructions. The session has no tools, shell execution, file-operation capability or cloud fallback. This follows Apple's guidance on separating untrusted input and validating output in [Improving the safety of generative model output](https://developer.apple.com/documentation/FoundationModels/improving-the-safety-of-generative-model-output). The small excerpt budget and fresh per-question session account for the shared instructions/input/output context described in [TN3193: Managing the on-device foundation model’s context window](https://developer.apple.com/documentation/Technotes/tn3193-managing-the-on-device-foundation-model-s-context-window); a context/model error is surfaced, not answered from external knowledge.

Files are read off the main actor with descriptor-relative, no-follow, bounded reads, not memory mapping. Identity/change tokens are checked around reads, after vector preparation, before follow-up resolution, after retrieval, after answer generation and before revealing a source. Changed, removed or link-swapped sources invalidate the prepared documents, vectors and conversation and require another read. This is a read-only freshness check, not a lock preventing later changes by another app.

## Availability, controls and privacy

- Requires an Apple Intelligence-capable Mac with Apple's on-device model ready. No API key, Gemini/Qwen download, server integration or separately purchased model is required. This uses `SystemLanguageModel(useCase: .general)`, not a cloud model.
- **AI Settings → Enable Document Questions** is independent of Smart Rename and Ask AI/Smart Search. Disabling it cancels work and clears prepared documents/answers, even if the tool is closed.
- Source text (including OCR), passage vectors, retrieved passages, questions and answers live only in the current Explorer window's memory. There is no persistent content index, chat database or app-created disk cache. No startup analysis occurs. Temporary page rasters are held only while processing the selected scan.
- Closing the panel cancels work and preserves completed context for reopening. **New Conversation** forgets questions/answers but retains the selected documents and their prepared vectors. **Clear**, changing the selection, disabling Document Questions, or closing the Explorer window forgets both. Pending completion cannot repopulate cleared data; temporary inputs of an in-flight read/model request can remain until cancellation unwinds. This is not a secure-memory-erasure guarantee, and ordinary system memory management is outside this app-level lifetime guarantee.
- Existing FFF/grep, Spotlight search, rename, copy/paste, grid, USB, comparison, duplicates, organization and offline catalogs retain their own workflows. AI cannot execute file-changing operations from a document answer.

## Validation

XcodeBuildMCP is used for builds and tests. Tests include synthetic real PDF page extraction and actual on-device answer generation, missing-fact handling, forged quote/ID rejection, byte/page/total-text limits, malformed input and symlinks, source mutation during inference/reveal, explicit read approval, selection changes, cancellation, opt-out and late completion. The macOS UI scenario reads a synthetic selected document, asks a real on-device question, opens its citation and clears the context while checking the original file is unchanged.

Light/dark document-answer, citation, photo-description and AI-settings layouts are rendered into offscreen windows. No unrelated desktop screenshots are captured. SwiftUI, Swift Concurrency and Swift Testing guidance informed accessibility-labelled themed controls, off-main bounded work, cancellation ownership and deterministic gate-based tests.

Validated on 2026-09-08 using XcodeBuildMCP: **521 unit/integration/presentation tests and 21 distinct selected macOS UI scenarios passed across the final regression batches**. Actual on-device integration tests ran; none were skipped. Coverage includes normal name/content search, copy/paste, rename, New Folder cancellation, grid reset, USB listing/eject, comparison, duplicates, organization, offline catalogs, visual analysis, natural photo routing, source-cited answers and AI preference persistence.

- Broad regression: **540 passed, 0 failures/skips** (521 + 19 UI), bundle `test_macos_2026-09-08T16-30-05-294Z_pid48916_00aef492.xcresult`.
- Final production-code recheck after error-copy, citation-contrast and accessibility polish: all **521 unit tests**, both feature UI flows and document opt-out passed, bundle `test_macos_2026-09-08T16-38-25-058Z_pid53163_a2c6c203.xcresult`. An older Smart Rename preference test did not reach the Rename sheet through its context-menu click after the Settings-window focus transition. Its setting/persistence assertions were retained and the entry path changed to the already-tested native **File → Rename** command; this is not a claim that the original right-click attempt passed.
- Final preference recheck: **2 passed, 0 failures/skips** (Smart Rename persistence + document opt-out while closed/relaunch), bundle `test_macos_2026-09-08T16-43-06-826Z_pid55502_dd07e3d1.xcresult`.

The photo tests verify the actual model's concept interpretation and synthetic Vision/OCR execution; they do not establish real-world scene recall or Siri parity. Document tests establish extraction, citation existence and lifecycle behavior, not universal semantic correctness of generated statements.

### September 9 accuracy and interaction regressions

- A real multi-document test exposed an answer that appended an unrelated invoice's date. Subject-specific instructions and source filenames now keep the response focused on the invoice requested, tested in both document orders. Another real-model test checks that an instruction embedded in an excerpt does not override the invoice's stated date. These are narrow regression cases, not a general semantic-correctness or prompt-injection-proof guarantee.
- Four CC0 photographs exercise actual Vision classification: both seaside photographs must be returned, and the forest and city must be excluded. Separate generated EXIF fixtures verify capture dates. Scene/date/type filtering and exact follow-ups are described in [Visual Search](VISUAL_SEARCH.md).
- The right-click Rename problem noted in the September 8 record was traced to the plain button's hit area, not the Settings preference. The full rounded menu row is now inside its button label; center and trailing-edge clicks have dedicated UI regressions, including after closing Settings.
- Restored **Edit → Select All / Cmd+A** through the standard responder chain. Replacing SwiftUI's [pasteboard command group](https://developer.apple.com/documentation/swiftui/commandgroupplacement/pasteboard) had removed Select All along with the stock clipboard items. The photo refinement UI test checks complete keyboard replacement of the query, rather than accepting appended text.
- Photo filter/evidence accessibility values are explicitly bound to the applied plan, preventing the selectable text's outer accessibility node from retaining the previous filter value.

The first unit/integration/layout and photo-flow recheck on September 9 passed **534 tests, 0 failures/skips** (533 Swift Testing tests + the new photo date/type follow-up UI scenario), XcodeBuildMCP bundle `test_macos_2026-09-09T19-12-06-877Z_pid15655_504c9b81.xcresult`. The actual on-device model and four-photo Vision corpus ran, not mocks alone. Filtered photo layouts were inspected in both light and dark themes.

A broader fixture-only UI run then reported **41 passed, 5 failed, 1 intentionally skipped** (Nearby is disabled), bundle `test_macos_2026-09-09T19-14-16-346Z_pid15992_3eb24c48.xcresult`. This was not a clean run. Follow-up investigation addressed the following:

- Smart Search now focuses the input before showing an empty results popover, and editing an existing query reopens dismissed results. Typing alone still does not invoke the model; Return/Find remains required.
- Sidebar collapse notifications repair unexpected native collapse while preserving explicit toolbar hiding and the existing 210–280 pt bounds. The native split controller's delegate is not replaced.
- Native List rows now vend drag item providers, avoiding a second drag turning into selection after a copy refresh. The regression performs two consecutive transfers, including a drop on an existing destination file, and checks that original files remain unchanged. The approach uses the native row-provider/on-insert pairing described in Apple's [macOS drag-and-drop walkthrough](https://developer.apple.com/videos/play/wwdc2021/10289/).
- The uninstall UI test scrolls its variable-height menu (installed terminal choices affect the height) before clicking. The preview test identifies the viewport by its text-view child; its content and size assertions remain. Sidebar width is measured on the visible scroll viewport rather than the outline's two-pixel overscan.

Final production-code UI recheck: **16 selected macOS UI tests passed, 0 UI failures/skips**, bundle `test_macos_2026-09-09T19-53-23-776Z_pid20044_ff39c11b.xcresult`. This covers photo date/type follow-ups, real source-cited document answers, all three Rename hit-area cases, Smart/normal name/content search and keyboard reveal, successive file drops, copy/paste, preview, uninstall confirmation, Shift/Delete and bounded sidebar resizing/toggling. The combined run still reported one new sidebar **unit-fixture geometry** assertion failure; it is not recorded as an entirely green bundle. The fixture now locates the actual column containing the attachment instead of assuming the split view's first subview is that column.

Final clean recheck after that fixture correction: **535 passed, 0 failures/skips** — all **534 unit/integration/presentation tests** plus the successive-drop UI case with an additional copied-JSON byte-equality assertion — bundle `test_macos_2026-09-09T20-00-48-776Z_pid20636_944483c7.xcresult`. Production code is unchanged from the 16-UI-test batch above. Together the final batches validate 534 unit/integration tests and 16 distinct UI scenarios; the full UI target was not rerun after the fixes. No temporary diagnostics or delegate-proxy experiment remains in production code.

### September 12 follow-up and semantic retrieval regressions

- Added bounded document follow-up context, visible question interpretation, New Conversation, and memory-only hybrid retrieval. Offscreen light/dark layouts were inspected using synthetic documents only.
- Real on-device invoice follow-up, subject switch and independent policy question passed in the focused run `test_macos_2026-09-12T12-42-05-041Z_pid93500_e71228a6.xcresult` (6 unit/integration tests passed). The three selected UI scenarios did **not** run: macOS requested user authentication for **Enable UI Automation** and timed out.
- The expanded unit/integration/presentation run `test_macos_2026-09-12T12-45-42-684Z_pid95214_5e77067b.xcresult` reported **550 passed, 2 failed, 0 skipped**. One failure was an ambiguous pronoun selecting an invoice; the deterministic plural-reference guard addresses that case. The other was `How do I end my membership?` not retrieving the subscription-cancellation passage (cosine similarity about 0.232, below the original 0.24 cutoff). A 0.22 retrieval cutoff now includes it. Refund, vacation and parking paraphrases and four unrelated-query exclusions are also checked. The 300,000-character preparation fixture takes about 19 seconds on this Mac; subsequent questions reuse the prepared document vectors.
- Some intermediate attempts built successfully but stalled before test execution in XCTest's IDE-session handshake. Only the agent's own test processes were stopped; no automation permissions, authentication settings or system daemons were changed. The user manually approved **Enable UI Automation**, after which UI execution resumed.
- The next broad run `test_macos_2026-09-12T13-18-05-204Z_pid5005_a903cc30.xcresult` passed all **553 unit/integration/presentation tests** and both existing document UI cases, but the new follow-up UI case failed its exact input assertion. The test helper clicked after Cmd+A and collapsed selection, appending the follow-up. The test now explicitly deletes the selected old query and checks the complete new value. Selectable answer text is read through its macOS accessibility **value**, not its empty label. Neither failure is hidden by removing the content assertions.
- The broader fixture-only UI run `test_macos_2026-09-12T13-22-11-997Z_pid6714_8f4ddc20.xcresult` passed **554 unit/integration/presentation tests and 17 of 18 selected UI scenarios**. The new follow-up test could observe an older answer while the next request was still running; it now waits for the new interpretation and checks both the initial date and the follow-up amount.
- Stronger assertions then exposed a real requested-fact error, reproduced outside UI: when a due date was followed by an amount in the same excerpt, the model answered the date question with `480 USD`. A quote's existence alone could not reject that answer. The answer prompt now emphasizes the requested attribute as well as its subject and places the separately encoded current question after the untrusted excerpts. A real-model regression asks for both date and amount with both source-text orders. This is a targeted correction, not a general entailment validator. The diagnostic run `test_macos_2026-09-12T13-30-12-754Z_pid8262_0ad1a00f.xcresult` was not green; it also had two UI entry failures during focus/external-keyboard interference. The user subsequently agreed to leave input idle for the UI recheck.

Final September 12 verification:

- **555 unit/integration/presentation tests passed, 0 failures, 0 skipped**, bundle `test_macos_2026-09-12T13-34-04-827Z_pid9030_7ef861c3.xcresult`. The actual on-device model and English sentence embedding ran. This includes requested-fact/source-order checks, subject switching, paraphrases and unrelated queries, byte-bounded Unicode context, cancellation/late completion, and light/dark layouts.
- **All 3 document UI scenarios passed, 0 failures, 0 skipped**, bundle `test_macos_2026-09-12T13-36-02-429Z_pid9574_b013d23f.xcresult`. The new scenario checks the initial invoice date, replaces the input with `How much is it?`, waits for the visible interpretation and the `480` answer, opens its citation, starts a fresh conversation without rereading, and verifies that the source file is unchanged. Existing explicit-read/clear and closed-panel opt-out/persistence scenarios also pass.
- Across these final recheck batches, **555 unit/integration/presentation tests and 18 distinct selected UI scenarios** have passing coverage. The 15 unchanged non-document UI workflows passed in the broader batch above: normal/Smart name and content search, keyboard reveal, copy/paste, repeated cross-pane drops, code/JSON preview, Rename after Settings, New Folder cancellation, grid reset, Shift/Delete, bounded sidebar resizing, USB listing/eject, and photo date/type follow-ups. Only the document answer prompt changed in production afterward, and its three UI cases were rerun. The full UI target was not rerun. The runner still reports AppKit/QoS and drag-type declaration diagnostics; passing tests do not establish a warning-free runtime or universal answer accuracy.

### September 12 scanned-PDF OCR

- Added explicit-read Vision OCR only for PDF pages without embedded text, with bounded serial rasterization, original page numbers, progress, OCR warnings, cancellation and unchanged-source validation. No file-changing action or persistent content index was added. English clear-print scans are the initial scope, not a claim of arbitrary scan accuracy.
- The first focused executable run reported **23 passed, 1 failed**, bundle `test_macos_2026-09-12T13-54-19-527Z_pid14840_bbb7279a.xcresult`. Real OCR misread a synthetic invoice's date. An offscreen fixture inspection showed the page was drawn small in the center of a high-resolution bitmap: the PDF drawing transform alone did not upscale it. Explicit point-to-pixel scaling fixes that defect. The exact date assertion was retained, and pixel-bound tests now cover cropping and all four PDF metadata rotations.
- The corrected focused run passed **24 tests, 0 failures/skips**, bundle `test_macos_2026-09-12T14-00-27-272Z_pid16763_72d7574b.xcresult`. Native PDF/TXT inputs bypass OCR. Mixed native/scanned/blank pages preserve provenance; clear/opt-out and changed files reject late data; per-page, per-file and selection limits are enforced.
- The expanded regression passed **571 unit/integration/presentation tests, 0 failures/skips**, bundle `test_macos_2026-09-12T14-04-58-888Z_pid18346_6100db29.xcresult`. This includes the real on-device model answering from OCR text on PDF page two, the 20-page success boundary, aggregate native/OCR text limits, and existing follow-up/retrieval/file-operation suites. Five-document answer and OCR citation layouts were inspected in light and dark using only synthetic offscreen fixtures.
- After singular/plural OCR-summary polish, all **28 selected OCR, real-model and presentation tests passed, 0 failures/skips**, bundle `test_macos_2026-09-12T14-08-31-989Z_pid19728_ea01429b.xcresult`. No production behavior changed after the full 571-test run beyond that summary label.
- After the user confirmed keyboard/mouse availability, the first interactive run reported **3 passed, 1 failed, 0 skipped**, bundle `test_macos_2026-09-12T14-13-17-382Z_pid21512_77e294d3.xcresult`. Normal document reading/answering/clearing, follow-up/New Conversation, and closed-panel opt-out/persistence all passed. The new OCR scenario generated an answer, opened its page-two source and found the recognition warning, but its heading assertion read the static text's empty accessibility label instead of its value. The UI test now checks the complete value `Scanned Invoice.pdf · page 2 · OCR`, retaining the filename/page/provenance check. Swift Testing guidance keeps UI tests in XCTest; no production code changed for this correction.
- The OCR scenario then passed **1 test, 0 failures/skips**, bundle `test_macos_2026-09-12T14-16-09-367Z_pid22210_e4b48662.xcresult`. It verifies explicit reading, real OCR/answer generation, the page-two citation and warnings, Clear, and original-file byte equality. Together these rechecks provide passing coverage for **all 4 distinct document UI scenarios** against the OCR implementation, in addition to the 571 unit/integration/presentation tests and final 28-test focused recheck above. The full UI target was not rerun. The existing AppKit main-thread diagnostic still appears; passing flows are not a warning-free runtime guarantee.
